'use client';
import { Form, Input, Button, Card, Typography } from 'antd';
import { createClient } from '@/lib/supabase';
import { useRouter } from 'next/navigation';
export default function LoginPage(){const router=useRouter(); const submit=async(v:{email:string;password:string})=>{const s=createClient(); const {error}=await s.auth.signInWithPassword(v); if(error){Typography.Text; return} router.push('/')}; return <main className="baytna-page auth-page"><Card className="auth-card"><Typography.Title level={2}>🏠 بيتنا</Typography.Title><Typography.Paragraph>سجّل دخولك علشان تحفظ قوائمك ومشترياتك وأسعارك.</Typography.Paragraph><Form layout="vertical" onFinish={submit}><Form.Item label="البريد الإلكتروني" name="email" rules={[{required:true,type:'email'}]}><Input /></Form.Item><Form.Item label="كلمة المرور" name="password" rules={[{required:true,min:6}]}><Input.Password /></Form.Item><Button type="primary" htmlType="submit" block>تسجيل الدخول</Button><Button type="link" block onClick={()=>router.push('/signup')}>إنشاء حساب جديد</Button></Form></Card></main>}
