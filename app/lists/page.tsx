'use client';
import { useEffect, useMemo, useState } from 'react';
import { Button, Card, Checkbox, Input, Modal, Select, Tag, Typography, InputNumber, Space, message, Divider } from 'antd';
import { PlusOutlined, ShoppingCartOutlined, CheckCircleOutlined } from '@ant-design/icons';
import { createClient } from '@/lib/supabase';

type Product=any; type List=any; type Item=any;
export default function ListsPage(){
 const [lists,setLists]=useState<List[]>([]),[active,setActive]=useState<List>(),[items,setItems]=useState<Item[]>([]),[products,setProducts]=useState<Product[]>([]),[open,setOpen]=useState(false),[name,setName]=useState(''),[selected,setSelected]=useState<string>(),[qty,setQty]=useState(1),[store,setStore]=useState<string>(),[stores,setStores]=useState<any[]>([]),[finishing,setFinishing]=useState(false);
 const load=async()=>{const s=createClient();const {data}=await s.from('shopping_lists').select('*').order('updated_at',{ascending:false});setLists(data??[])};
 useEffect(()=>{load();(async()=>{const s=createClient();const [{data:p},{data:st}]=await Promise.all([s.from('product_variants').select('id,variant_name_ar,variant_name_en,amount,unit,brands(name_ar,name_en)').order('variant_name_ar').limit(1000),s.from('stores').select('*').order('name')]);setProducts(p??[]);setStores(st??[])})()},[]);
 const select=async(l:List)=>{setActive(l);const s=createClient();const {data}=await s.from('shopping_list_items').select('id,custom_name,requested_quantity,purchased,purchased_quantity,unit_price,total_price,product_variant_id,product_variants(variant_name_ar,variant_name_en,amount,unit)').eq('list_id',l.id);setItems(data??[])};
 const add=async()=>{if(!name.trim())return;const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user){message.info('سجّل الدخول أولاً');return}const {error}=await s.from('shopping_lists').insert({user_id:user.id,name:name.trim()});if(error)message.error(error.message);else{setName('');setOpen(false);load()}};
 const addItem=async()=>{if(!active||!selected)return;const s=createClient();const {error}=await s.from('shopping_list_items').insert({list_id:active.id,product_variant_id:selected,requested_quantity:qty});if(error)message.error(error.message);else{message.success('تمت الإضافة');select(active);setSelected(undefined);setQty(1)}};
 const updateItem=async(i:Item, patch:any)=>{const s=createClient();const {error}=await s.from('shopping_list_items').update(patch).eq('id',i.id);if(error)message.error(error.message);else if(active)select(active)};
 const startShopping=async()=>{if(!active)return;const s=createClient();const {error}=await s.from('shopping_lists').update({status:'shopping'}).eq('id',active.id);if(error)message.error(error.message);else{setActive({...active,status:'shopping'});load();message.success('بدأنا التسوق 🛒')}};
 const finishShopping=async()=>{if(!active)return;setFinishing(true);const s=createClient();const purchased=items.filter(i=>i.purchased);if(!purchased.length){message.warning('علّم على الأقل منتج واحد اشتريته');setFinishing(false);return}const {data:{user}}=await s.auth.getUser();if(!user){setFinishing(false);return}const {data:session,error:sessionError}=await s.from('shopping_sessions').insert({list_id:active.id,store_id:store||null,total:purchased.reduce((a,i)=>a+Number(i.total_price||0),0)}).select().single();if(sessionError){message.error(sessionError.message);setFinishing(false);return}for(const i of purchased){const {data:pi,error:e}=await s.from('purchase_items').insert({user_id:user.id,shopping_session_id:session.id,product_variant_id:i.product_variant_id,store_id:store||null,quantity:i.purchased_quantity||i.requested_quantity||1,unit_price:i.unit_price,total_price:i.total_price}).select().single();if(!e&&pi){await s.from('price_history').insert({user_id:user.id,product_variant_id:i.product_variant_id,store_id:store||null,purchase_item_id:pi.id,quantity:pi.quantity,unit_price:pi.unit_price,total_price:pi.total_price})}}await s.from('shopping_sessions').update({completed_at:new Date().toISOString()}).eq('id',session.id);await s.from('shopping_lists').update({status:'completed'}).eq('id',active.id);message.success('تم حفظ المشتريات والأسعار 🎉');setFinishing(false);load();select({...active,status:'completed'})};
 const options=useMemo(()=>products.map(p=>({value:p.id,label:`${p.variant_name_ar||p.variant_name_en} — ${p.amount} ${p.unit}${p.brands?.name_ar?` — ${p.brands.name_ar}`:''}`})),[products]);
 const total=items.reduce((a,i)=>a+Number(i.total_price||0),0);
 return <main className="baytna-page"><div className="baytna-container"><section className="hero"><h1>قوائم المشتريات</h1><p>اعمل الليستة، وبعدها ابدأ التسوق وسجل اللي اشتريته وأسعاره.</p></section><div style={{display:'grid',gridTemplateColumns:'minmax(260px,.8fr) minmax(320px,1.5fr)',gap:16}}><Card title="قوائمك" extra={<Button type="primary" icon={<PlusOutlined/>} onClick={()=>setOpen(true)}>جديدة</Button>}>{lists.map(l=><div key={l.id} className="list-row" onClick={()=>select(l)} style={{cursor:'pointer'}}><span>{l.name}</span><Tag color={l.status==='shopping'?'orange':l.status==='completed'?'blue':'green'}>{l.status==='shopping'?'تسوق':l.status==='completed'?'مكتملة':'مسودة'}</Tag></div>)}{!lists.length&&<Typography.Text type="secondary">مفيش قوائم لسه.</Typography.Text>}</Card><Card title={active?.name||'اختار قائمة'} extra={active&&active.status!=='completed'&&<Button icon={<ShoppingCartOutlined/>} onClick={startShopping}>ابدأ التسوق</Button>}>{active&&<><Space.Compact style={{width:'100%',marginBottom:14}}><Select showSearch optionFilterProp="label" style={{flex:1}} placeholder="ابحث عن منتج" options={options} value={selected} onChange={setSelected}/><InputNumber min={1} value={qty} onChange={v=>setQty(Number(v)||1)} /><Button type="primary" icon={<PlusOutlined/>} onClick={addItem}>إضافة</Button></Space.Compact>{items.map(i=><div className="list-row" key={i.id}><Checkbox checked={i.purchased} onChange={e=>updateItem(i,{purchased:e.target.checked,purchased_quantity:e.target.checked?(i.purchased_quantity||i.requested_quantity):null})}><span style={{textDecoration:i.purchased?'line-through':'none'}}>{i.custom_name||i.product_variants?.variant_name_ar}</span></Checkbox><Space><InputNumber min={0} placeholder="الكمية" value={i.purchased_quantity??undefined} onChange={v=>updateItem(i,{purchased_quantity:v})} disabled={!i.purchased} style={{width:90}}/><InputNumber min={0} placeholder="سعر الوحدة" value={i.unit_price??undefined} onChange={v=>updateItem(i,{unit_price:v,total_price:(Number(v)||0)*(Number(i.purchased_quantity||i.requested_quantity)||1)})} disabled={!i.purchased} style={{width:110}}/><span>{i.purchased?`${Number(i.total_price||0).toFixed(2)} EGP`: `${i.requested_quantity} ${i.product_variants?.unit}`}</span></Space></div>)}{active.status==='shopping'&&<><Divider/><Space wrap><Select allowClear placeholder="المحل" value={store} onChange={setStore} options={stores.map(x=>({value:x.id,label:x.name}))} style={{minWidth:180}}/><Button type="primary" icon={<CheckCircleOutlined/>} loading={finishing} onClick={finishShopping}>خلصت التسوق</Button><b>الإجمالي: {total.toFixed(2)} EGP</b></Space></>}{active&&!items.length&&<Typography.Text type="secondary">القائمة فاضية — اختار منتج وأضفه.</Typography.Text>}</>}</Card></div></div><Modal title="قائمة جديدة" open={open} onCancel={()=>setOpen(false)} onOk={add} okText="إنشاء" cancelText="إلغاء"><Input value={name} onChange={e=>setName(e.target.value)} placeholder="مشتريات الأسبوع" /></Modal></main>}
